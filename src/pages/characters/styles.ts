import styled from "styled-components";

export const Wrapper = styled.section`
  max-width: 800px;
  margin: 1em auto;

  .character-list {
    &__title {
      font-size: 2em;
      text-align: center;
    }
  }
`;
